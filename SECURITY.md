# Security Policy

HTTP from warehouse SQL can expose secrets and move data outside your warehouse.
Use warehouse-managed secrets, allowlisted destinations, and auditable connection
objects whenever possible.

Do not report secrets in public issues. Use the repository security advisory flow
or contact the maintainers privately.
